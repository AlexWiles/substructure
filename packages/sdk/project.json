{
  "name": "@substructure.ai/sdk",
  "targets": {
    "build": {
      "command": "pnpm build",
      "options": {
        "cwd": "packages/sdk"
      },
      "inputs": [
        "{projectRoot}/src/**",
        "{projectRoot}/tsup.config.ts",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/package.json"
      ],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
