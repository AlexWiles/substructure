{
  "$defs": {
    "AgentConfig": {
      "additionalProperties": false,
      "description": "A declared agent identity — the same shape whether it is written in an\n`[agent.<id>]` section or returned by a worker.\n\n`llm` names the `[llm.*]` block every proposed call runs on, and so decides\nboth the venue (the engine with a vendor key, or the agent's own worker) and\nthe wire shape of a worker-run call. It is effectively required: a config\nthat names none fails when the engine resolves a call against it.",
      "properties": {
        "defer_tools": {
          "anyOf": [
            {
              "$ref": "#/$defs/DeferToolsWire"
            },
            {
              "type": "null"
            }
          ],
          "description": "Defer every tool this agent offers, from any source, unless the tool or\nthe connection says otherwise. Absent ⇒ the agent defers nothing of its\nown; a connection may still defer on its own account.\n\nPresence is the switch, so an agent cannot carry settings that do\nnothing. Declared on the agent because an agent can hold this opinion\nbefore it names a connection: one that sets it gets the search tools\nfrom its first turn, so a connection added later costs no cache."
        },
        "effort": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReasoningEffort"
            },
            {
              "type": "null"
            }
          ],
          "description": "How hard the model thinks, carried on the agent because it pairs with\nthe model. Unset sends no reasoning config and leaves the provider its\nown default."
        },
        "llm": {
          "description": "The `[llm.*]` block this agent's calls run on.",
          "type": [
            "string",
            "null"
          ]
        },
        "mcp": {
          "description": "MCP servers",
          "items": {
            "$ref": "#/$defs/McpServer"
          },
          "type": "array"
        },
        "mcp_announce": {
          "$ref": "#/$defs/McpAnnounce",
          "description": "Where the engine tells the model that an MCP server is available, and\nwhat that server says it is for.\n\nSeparate from `defer_tools`: a server exists whether or not its tools\nare deferred, and where a notice lands is a fact about this agent's\nprompt rather than about any server."
        },
        "model": {
          "type": "string"
        },
        "plugins": {
          "description": "Plugins this agent uses.",
          "items": {
            "$ref": "#/$defs/AgentPlugin"
          },
          "type": "array"
        },
        "retry": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Boxed: five per-kind overrides is a lot of bytes to carry inline\nthrough every command that holds a config."
        },
        "sub_agents": {
          "description": "Sub-agents the model can delegate to. Presented to the model as tools (by\nid) alongside `tools`, but each call spawns a child session rather than\nexecuting a function.",
          "items": {
            "$ref": "#/$defs/SubAgent"
          },
          "type": "array"
        },
        "system": {
          "type": [
            "string",
            "null"
          ]
        },
        "tools": {
          "description": "Worker- or client-executed tools the model can call.",
          "items": {
            "$ref": "#/$defs/AgentTool"
          },
          "type": "array"
        }
      },
      "required": [
        "model"
      ],
      "title": "AgentConfig",
      "type": "object"
    },
    "AgentPlugin": {
      "additionalProperties": false,
      "description": "A plugin an agent uses. The skills and servers are stamped from the bundle\nwhen the config loads. To enable a plugin, write it into the config.",
      "properties": {
        "approve": {
          "$ref": "#/$defs/Approve"
        },
        "auth_failure": {
          "$ref": "#/$defs/McpAuthFailure"
        },
        "description": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "servers": {
          "description": "Where each of this plugin's servers is declared.",
          "items": {
            "$ref": "#/$defs/ConnectionPath"
          },
          "type": "array"
        },
        "skills": {
          "items": {
            "$ref": "#/$defs/SkillMeta"
          },
          "type": "array"
        },
        "tool_sync_failure": {
          "$ref": "#/$defs/McpToolSyncFailure"
        },
        "tools": {
          "anyOf": [
            {
              "$ref": "#/$defs/McpTools"
            },
            {
              "type": "null"
            }
          ],
          "description": "Applied to each of the plugin's servers."
        }
      },
      "required": [
        "id"
      ],
      "title": "AgentPlugin",
      "type": "object"
    },
    "AgentTool": {
      "additionalProperties": false,
      "description": "A function tool the agent offers. The model-facing contract is\n`name`/`description`/`input`/`output`; `handler` selects where a call runs —\n`Some(Client)` ⇒ client-executed, absent ⇒ worker-executed (the default).\n`server` is invalid for tools: engine-executed tools come from a connector,\nwhich a worker declares by id rather than by tool.",
      "properties": {
        "defer": {
          "description": "Keep this tool out of the request. See [`LlmTool::defer`]. Absent ⇒\nthe agent's `defer_tools`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "description": {
          "type": "string"
        },
        "handler": {
          "anyOf": [
            {
              "$ref": "#/$defs/Handler"
            },
            {
              "type": "null"
            }
          ]
        },
        "input": {},
        "name": {
          "type": "string"
        },
        "output": {}
      },
      "required": [
        "name"
      ],
      "title": "AgentTool",
      "type": "object"
    },
    "Approve": {
      "description": "Which of a connection's calls stop for a person.",
      "oneOf": [
        {
          "enum": [
            "never",
            "always"
          ],
          "type": "string"
        },
        {
          "const": "destructive",
          "description": "A tool that the connection marks `destructiveHint`.",
          "type": "string"
        }
      ],
      "title": "Approve"
    },
    "AudioData": {
      "additionalProperties": false,
      "properties": {
        "data": {
          "type": "string"
        },
        "format": {
          "type": "string"
        }
      },
      "required": [
        "data",
        "format"
      ],
      "title": "AudioData",
      "type": "object"
    },
    "ClientContext": {
      "additionalProperties": false,
      "description": "Inputs a client declares on its run (the AG-UI `tools`/`context`/`state`/\n`forwardedProps`), forwarded to the worker on the `client.messages` decision.\n`tools` are the browser's frontend tools, normalized to client-handled\n[`AgentTool`]s; the engine layers them onto the proposed config by default, and\nthe worker may override (e.g. whitelist) by returning its own `agent`.",
      "properties": {
        "context": {
          "items": {},
          "type": "array"
        },
        "forwarded_props": {},
        "state": {},
        "tools": {
          "items": {
            "$ref": "#/$defs/AgentTool"
          },
          "type": "array"
        }
      },
      "title": "ClientContext",
      "type": "object"
    },
    "ClientInput": {
      "description": "Everything a client can send on the input surface: submit a message / a full view / an\nappend batch / a named action, resume an interrupt, or settle a client tool. A flat,\ninternally-tagged union — its seven tags produce serde's \"unknown variant, expected one\nof …\" error for free. `Runtime::handle_client_input` is the single seam that dispatches\nit (mirroring `resolve_response` on the worker side).\n\nAddressing lives where it is meaningful, not in a shared envelope: `agent_id` (routes\nthe turn, creating the session if new) and the optional idempotency `turn_id` are\nfields of the four submit variants only. A resume/settle addresses an interrupt/effect\nid and continues whatever turn is active, so it carries neither — misplacing them is\nunrepresentable rather than rejected. `session_id` is the one universal address and\nrides the envelope. A submit's body rebuilds a [`ClientPayload`] at the seam.",
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "message": {
              "$ref": "#/$defs/DraftMessage"
            },
            "queue": {
              "description": "Hold this message for the next turn instead of refusing it when one\nis already running. Off by default: rejection stays the contract for\na plain submitter, and queuing is declared intent.",
              "type": "boolean"
            },
            "stream": {
              "type": "boolean"
            },
            "turn_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "client.message",
              "type": "string"
            }
          },
          "required": [
            "type",
            "agent_id",
            "message"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "client": {
              "$ref": "#/$defs/ClientContext"
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/DraftMessage"
              },
              "type": "array"
            },
            "stream": {
              "type": "boolean"
            },
            "turn_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "client.messages",
              "type": "string"
            }
          },
          "required": [
            "type",
            "agent_id",
            "messages"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "client": {
              "$ref": "#/$defs/ClientContext"
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/DraftMessage"
              },
              "type": "array"
            },
            "queue": {
              "description": "Hold this batch for the next turn instead of refusing it when one is\nalready running. Off by default: rejection stays the contract for a\nplain submitter, and queuing is declared intent.",
              "type": "boolean"
            },
            "stream": {
              "type": "boolean"
            },
            "turn_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "client.append",
              "type": "string"
            }
          },
          "required": [
            "type",
            "agent_id",
            "messages"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "args": {},
            "name": {
              "type": "string"
            },
            "turn_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "client.action",
              "type": "string"
            }
          },
          "required": [
            "type",
            "agent_id",
            "name"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The body of an interrupt resume: which interrupt, and the payload delivered\nto the worker. Shared by the [`ClientInput::InterruptResume`] input and the\n[`DecisionTrigger::InterruptResumed`] trigger.",
          "properties": {
            "interrupt_id": {
              "type": "string"
            },
            "payload": {},
            "type": {
              "const": "interrupt.resume",
              "type": "string"
            }
          },
          "required": [
            "type",
            "interrupt_id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "content": {
              "items": {
                "$ref": "#/$defs/ToolContent"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "id": {
              "type": "string"
            },
            "is_error": {
              "type": "boolean"
            },
            "result": {},
            "structured_content": {},
            "type": {
              "const": "tool.result",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "error": {
              "type": "string"
            },
            "id": {
              "type": "string"
            },
            "retryable": {
              "type": "boolean"
            },
            "type": {
              "const": "tool.error",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "error",
            "retryable"
          ],
          "type": "object"
        }
      ],
      "title": "ClientInput"
    },
    "ClientPayload": {
      "description": "The client→engine inbound *submit* wire form: an untrusted client submits a message,\nits full conversation view, an append batch, or a named action. Lowered to domain events at the\n`SubmitClientPayload` command seam (`runtime::session::command`); never persisted\nas-is. Carried verbatim inside [`ClientInput`], which is the full client input\nsurface.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "The body of a `client.message`: one message, optionally streamed.",
          "properties": {
            "message": {
              "$ref": "#/$defs/DraftMessage"
            },
            "stream": {
              "type": "boolean"
            },
            "type": {
              "const": "client.message",
              "type": "string"
            }
          },
          "required": [
            "type",
            "message"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The body of a `client.messages`: the client's full conversation view, optionally streamed.",
          "properties": {
            "client": {
              "$ref": "#/$defs/ClientContext"
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/DraftMessage"
              },
              "type": "array"
            },
            "stream": {
              "type": "boolean"
            },
            "type": {
              "const": "client.messages",
              "type": "string"
            }
          },
          "required": [
            "type",
            "messages"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The body of a `client.append`: messages appended at the session head. The\nview is composed against the active path at delivery, so a queued append\nlands after whatever turn beat it — it can never fork the tree. Messages\nwhose ids are already recorded are dropped.",
          "properties": {
            "client": {
              "$ref": "#/$defs/ClientContext"
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/DraftMessage"
              },
              "type": "array"
            },
            "stream": {
              "type": "boolean"
            },
            "type": {
              "const": "client.append",
              "type": "string"
            }
          },
          "required": [
            "type",
            "messages"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The payload of a `client.action`: a named action with optional JSON args.",
          "properties": {
            "args": {},
            "name": {
              "type": "string"
            },
            "type": {
              "const": "client.action",
              "type": "string"
            }
          },
          "required": [
            "type",
            "name"
          ],
          "type": "object"
        }
      ],
      "title": "ClientPayload"
    },
    "ConnectionPath": {
      "description": "Where a connection is declared: `mcp.<id>` or `plugin.<id>.mcp.<server>`",
      "type": "string"
    },
    "Content": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "items": {
            "$ref": "#/$defs/StoredContent"
          },
          "type": "array"
        }
      ],
      "description": "Message content: either a plain string or an array of typed parts.\nSerializes as a raw string or array respectively (untagged).",
      "title": "Content"
    },
    "ContentPart": {
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "text": {
              "type": "string"
            },
            "type": {
              "const": "text",
              "type": "string"
            }
          },
          "required": [
            "type",
            "text"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "image_url": {
              "$ref": "#/$defs/ImageUrl"
            },
            "type": {
              "const": "image_url",
              "type": "string"
            }
          },
          "required": [
            "type",
            "image_url"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "file": {
              "$ref": "#/$defs/FileData"
            },
            "type": {
              "const": "file",
              "type": "string"
            }
          },
          "required": [
            "type",
            "file"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "input_audio": {
              "$ref": "#/$defs/AudioData"
            },
            "type": {
              "const": "input_audio",
              "type": "string"
            }
          },
          "required": [
            "type",
            "input_audio"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "type": {
              "const": "video_url",
              "type": "string"
            },
            "video_url": {
              "$ref": "#/$defs/VideoUrl"
            }
          },
          "required": [
            "type",
            "video_url"
          ],
          "type": "object"
        }
      ],
      "title": "ContentPart"
    },
    "DecisionAction": {
      "description": "The action a worker authors on the wire. Mirrors the internal `Action`, but a\nsettle's effect id may be omitted: on the sync/pull paths the answered\n`*.execute` trigger names it, so echoing it is redundant. `resolve_response`\n(`runtime::session::wire`) turns this into the internal `Action` (id always\npresent) at the transport boundary.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "A flat, all-optional LLM request. `id` omitted ⇒ the engine mints one; it\nbecomes the assistant node's id. Omitted fields are filled from the agent\nconfig (merge source), then engine defaults; `messages` omitted ⇒\n`[config.system?] + the decision's declared view`. Explicit `messages`\nsuppress system injection. A bare `{\"type\":\"llm.call\"}` prompts per the\nagent's identity over the current view.",
          "properties": {
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "llm": {
              "description": "The `[llm.*]` block this call runs on; omitted ⇒ the merge source\nconfig's `llm`. Naming a different block moves one call to another\nvenue or vendor.",
              "type": [
                "string",
                "null"
              ]
            },
            "max_completion_tokens": {
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/DraftMessage"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "model": {
              "type": [
                "string",
                "null"
              ]
            },
            "reasoning": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ReasoningConfig"
                },
                {
                  "type": "null"
                }
              ]
            },
            "retry": {
              "anyOf": [
                {
                  "$ref": "#/$defs/RetryOverride"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Layered over the agent config's `llm` policy, else over the engine's\ndefault."
            },
            "stream": {
              "type": [
                "boolean",
                "null"
              ]
            },
            "temperature": {
              "format": "double",
              "type": [
                "number",
                "null"
              ]
            },
            "tools": {
              "items": {
                "$ref": "#/$defs/LlmTool"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "type": {
              "const": "llm.call",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "`id` omitted ⇒ the engine mints one (LLM-driven tools carry the model's id).\n\nThere is no `handler`: where a call runs follows from its name. A tool\nresolved from a connector runs on the engine, a tool declared\n`handler: client` runs on the client, and anything else runs on the\nworker. The engine already knows all three, so asking the worker to\nrestate it only creates a way for the two to disagree.",
          "properties": {
            "arguments": {},
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "type": "string"
            },
            "retry": {
              "anyOf": [
                {
                  "$ref": "#/$defs/RetryOverride"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Layered over the agent config's policy for this kind, else over the\nengine's default for where the tool runs."
            },
            "type": {
              "const": "tool.call",
              "type": "string"
            }
          },
          "required": [
            "type",
            "name",
            "arguments"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "`id`/`attempt` omitted ⇒ taken from the answering `tool.execute` trigger,\nfencing the result to the attempt that ran.",
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "content": {
              "items": {
                "$ref": "#/$defs/ToolContent"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "is_error": {
              "type": "boolean"
            },
            "result": {},
            "structured_content": {},
            "type": {
              "const": "tool.result",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "`id`/`attempt` omitted ⇒ taken from the answering `llm.execute` trigger,\nfencing the result to the attempt that ran.",
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "response": {
              "description": "A neutral `LlmResponse`, or the provider's native response when the\nanswered `llm.execute` carried a `format`."
            },
            "type": {
              "const": "llm.result",
              "type": "string"
            }
          },
          "required": [
            "type",
            "response"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "code": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ErrorCode"
                },
                {
                  "type": "null"
                }
              ]
            },
            "detail": {},
            "error": {
              "type": "string"
            },
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "retryable": {
              "description": "Omitted ⇒ terminal.",
              "type": "boolean"
            },
            "type": {
              "const": "tool.error",
              "type": "string"
            }
          },
          "required": [
            "type",
            "error"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "code": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ErrorCode"
                },
                {
                  "type": "null"
                }
              ]
            },
            "detail": {},
            "error": {
              "type": "string"
            },
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "retryable": {
              "description": "Omitted ⇒ terminal.",
              "type": "boolean"
            },
            "type": {
              "const": "llm.error",
              "type": "string"
            }
          },
          "required": [
            "type",
            "error"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "message": {
              "anyOf": [
                {
                  "$ref": "#/$defs/DraftMessage"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The child's opening message. It travels with the spawn, so it\ncannot race the creation of the session it opens."
            },
            "retry": {
              "anyOf": [
                {
                  "$ref": "#/$defs/RetryOverride"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Layered over the agent config's `sub_agent` policy, else over the\nengine's default."
            },
            "session_id": {
              "type": "string"
            },
            "tool_call_id": {
              "description": "The model tool-call this delegation answers — always required.",
              "type": "string"
            },
            "type": {
              "const": "sub_agent.spawn",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "agent_id",
            "tool_call_id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "message": {
              "$ref": "#/$defs/DraftMessage"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "message.send",
              "type": "string"
            }
          },
          "required": [
            "type",
            "session_id",
            "message"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "`interrupt_id` omitted ⇒ the engine mints one to correlate the later resume.",
          "properties": {
            "interrupt_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "payload": {},
            "reason": {
              "type": "string"
            },
            "type": {
              "const": "interrupt",
              "type": "string"
            }
          },
          "required": [
            "type",
            "reason"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Resolve an open interrupt and resume the session.",
          "properties": {
            "interrupt_id": {
              "type": "string"
            },
            "payload": {},
            "type": {
              "const": "interrupt.resolve",
              "type": "string"
            }
          },
          "required": [
            "type",
            "interrupt_id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Fetch a connection's tools again, after a person replaced its\ncredential.",
          "properties": {
            "path": {
              "$ref": "#/$defs/ConnectionPath"
            },
            "type": {
              "const": "connector.sync",
              "type": "string"
            }
          },
          "required": [
            "type",
            "path"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "data": {},
            "type": {
              "const": "done",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ],
      "title": "DecisionAction"
    },
    "DecisionRequest": {
      "additionalProperties": false,
      "properties": {
        "agent": {
          "anyOf": [
            {
              "$ref": "#/$defs/AgentConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "The agent config resolved for the active path (`null` when none is set)."
        },
        "agent_id": {
          "type": "string"
        },
        "ancestry": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "attempts": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "calls": {
          "items": {
            "$ref": "#/$defs/Effect"
          },
          "type": "array"
        },
        "deadline": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "decision_id": {
          "type": "string"
        },
        "identity": {
          "$ref": "#/$defs/WorkerIdentity"
        },
        "message_tree": {
          "$ref": "#/$defs/MessageTree"
        },
        "messages": {
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        },
        "pending_calls": {
          "description": "Count of in-flight `tool_call`/`sub_agent` calls.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "proposed": {
          "$ref": "#/$defs/DecisionResponse",
          "description": "The engine's default continuation for `trigger` (empty when it needs\nworker knowledge). Advisory: accept by echoing it as the decision."
        },
        "session_id": {
          "type": "string"
        },
        "state": {
          "$ref": "#/$defs/WorkerState"
        },
        "trigger": {
          "$ref": "#/$defs/DecisionTrigger"
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "session_id",
        "decision_id",
        "agent_id",
        "identity",
        "trigger",
        "proposed",
        "state",
        "calls",
        "pending_calls",
        "messages",
        "message_tree",
        "ancestry",
        "attempts"
      ],
      "title": "DecisionRequest",
      "type": "object"
    },
    "DecisionResponse": {
      "additionalProperties": false,
      "description": "A decision: the messages/actions to author, plus optional state/agent writes.\nThe worker returns one; the engine also proposes one as the default\ncontinuation (`DecisionRequest::proposed`), which the worker echoes or amends.",
      "properties": {
        "actions": {
          "items": {
            "$ref": "#/$defs/DecisionAction"
          },
          "type": "array"
        },
        "agent": {
          "anyOf": [
            {
              "$ref": "#/$defs/AgentConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "A new agent config write; omitted keeps the current config."
        },
        "channels": {
          "additionalProperties": {},
          "description": "How each channel shows this decision, keyed by channel kind (e.g.\n`slack`). Opaque to the engine.",
          "type": "object"
        },
        "messages": {
          "items": {
            "$ref": "#/$defs/DraftMessage"
          },
          "type": "array"
        },
        "state": {
          "anyOf": [
            {
              "$ref": "#/$defs/WorkerState"
            },
            {
              "type": "null"
            }
          ],
          "description": "Omitted or `null` keeps the current state; clear with a non-null empty value."
        }
      },
      "title": "DecisionResponse",
      "type": "object"
    },
    "DecisionTrigger": {
      "description": "The trigger a worker sees on the wire — the materialized projection of the\nengine's internal decision trigger. It has no `ClientMessage`: a bare client\nmessage is always materialized to `ClientTranscript` by `to_wire_trigger`\n(`runtime::session::wire`) before delivery, so an unmaterialized message can\nnever reach a worker.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "The first decision of every session; carries no proposal.",
          "properties": {
            "type": {
              "const": "session.start",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "client": {
              "$ref": "#/$defs/ClientContext",
              "description": "Inputs the client declared on its run; the engine layers `client.tools`\nonto the proposed config by default."
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/DraftMessage"
              },
              "type": "array"
            },
            "new_from": {
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "type": {
              "const": "client.messages",
              "type": "string"
            }
          },
          "required": [
            "type",
            "messages",
            "new_from",
            "client"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "args": {},
            "name": {
              "type": "string"
            },
            "type": {
              "const": "client.action",
              "type": "string"
            }
          },
          "required": [
            "type",
            "name"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Answer with `tool.result`/`tool.error`.",
          "properties": {
            "arguments": {
              "type": "string"
            },
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "deadline": {
              "format": "date-time",
              "type": [
                "string",
                "null"
              ]
            },
            "id": {
              "type": "string"
            },
            "input": {
              "$ref": "#/$defs/ToolInput",
              "description": "The engine's classification of `arguments` against the tool's\ndeclared `input` schema: `valid` (with the parsed `value`),\n`invalid` (value plus the violation), or `malformed` (not a JSON\nobject). Always on the wire."
            },
            "name": {
              "type": "string"
            },
            "type": {
              "const": "tool.execute",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "name",
            "arguments",
            "input",
            "attempt"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "error": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ErrorInfo"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "result": {
              "anyOf": [
                {
                  "$ref": "#/$defs/StoredResult"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "const": "tool.finished",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "ok",
            "name"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Answer with `llm.result`/`llm.error`.",
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "deadline": {
              "format": "date-time",
              "type": [
                "string",
                "null"
              ]
            },
            "format": {
              "anyOf": [
                {
                  "$ref": "#/$defs/LlmFormat"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "type": "string"
            },
            "request": {
              "description": "The neutral `LlmRequest` JSON, or the provider's native request body\nwhen `format` is set."
            },
            "stream": {
              "type": "boolean"
            },
            "type": {
              "const": "llm.execute",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "request",
            "stream",
            "attempt"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "cost": {
              "pattern": "^-?\\d+(\\.\\d+)?$",
              "type": [
                "string",
                "null"
              ]
            },
            "error": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ErrorInfo"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "type": "string"
            },
            "message": {
              "anyOf": [
                {
                  "$ref": "#/$defs/DraftMessage"
                },
                {
                  "type": "null"
                }
              ]
            },
            "ok": {
              "type": "boolean"
            },
            "refused": {
              "description": "True when the model declined the request rather than answering it.\nA refusal reads as a turn that stopped well and said nothing, so\nwithout this the run continues from a blank answer.",
              "type": "boolean"
            },
            "truncated": {
              "type": "boolean"
            },
            "type": {
              "const": "llm.finished",
              "type": "string"
            },
            "usage": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Usage"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "required": [
            "type",
            "id",
            "ok",
            "truncated"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "error": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ErrorInfo"
                },
                {
                  "type": "null"
                }
              ]
            },
            "id": {
              "type": "string"
            },
            "ok": {
              "type": "boolean"
            },
            "result": {
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "sub_agent.finished",
              "type": "string"
            }
          },
          "required": [
            "type",
            "id",
            "ok",
            "session_id",
            "agent_id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The body of an interrupt resume: which interrupt, and the payload delivered\nto the worker. Shared by the [`ClientInput::InterruptResume`] input and the\n[`DecisionTrigger::InterruptResumed`] trigger.",
          "properties": {
            "interrupt_id": {
              "type": "string"
            },
            "payload": {},
            "type": {
              "const": "interrupt.resumed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "interrupt_id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Fired after a turn completes, carrying its final output; blocks the session\ngoing idle until answered. Echo the proposed `done` to finalize.",
          "properties": {
            "cost": {
              "pattern": "^-?\\d+(\\.\\d+)?$",
              "type": [
                "string",
                "null"
              ]
            },
            "data": {},
            "turn_id": {
              "type": "string"
            },
            "type": {
              "const": "turn.finished",
              "type": "string"
            },
            "usage": {
              "$ref": "#/$defs/Usage"
            }
          },
          "required": [
            "type",
            "turn_id"
          ],
          "type": "object"
        }
      ],
      "title": "DecisionTrigger"
    },
    "DeferTools": {
      "additionalProperties": {
        "not": {}
      },
      "description": "How an agent's deferred tools reach the model.",
      "properties": {
        "max_matches": {
          "description": "The most matches one search answers with. Never zero: a search that can\nanswer with nothing is a search the model cannot use.",
          "format": "uint",
          "minimum": 1,
          "type": "integer"
        },
        "strategy": {
          "$ref": "#/$defs/DeferToolsStrategy",
          "description": "Which tools the agent gets to reach the ones it defers."
        }
      },
      "title": "DeferTools",
      "type": "object"
    },
    "DeferToolsStrategy": {
      "description": "How the tools an agent defers reach the model.\n\nThe engine holds every deferred definition whatever this says, and answers\nits own tools whatever this says. This chooses two things: which of those\ntools the request advertises, and whether the request carries the deferred\ndefinitions.\n\nDeclared on the agent, beside `defer_tools`: which tools an agent gets is\nthe agent's business, the same way as whether it defers at all.",
      "oneOf": [
        {
          "const": "search",
          "description": "`tool_search` and `call_tool`. A search answers with the schema, so one\nsearch is the whole distance to a call, and nothing hands the model a\nname it cannot then reach.",
          "type": "string"
        }
      ],
      "title": "DeferToolsStrategy"
    },
    "DeferToolsWire": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "#/$defs/DeferTools"
        }
      ],
      "description": "The two forms `defer_tools` accepts: `true` for the defaults, or a table.\n`false` reads the same as absent, so a config can turn off what it inherits.",
      "title": "DeferToolsWire"
    },
    "DraftMessage": {
      "additionalProperties": false,
      "description": "The wire form of a [`Message`]: `id` is optional because a client-submitted or\nworker-authored message is not yet recorded. `record`/`rerecord`\n(`runtime::session::wire`) are the seams that lower it to the internal\n[`Message`] (id always present) at recording time.",
      "properties": {
        "content": {
          "anyOf": [
            {
              "$ref": "#/$defs/Content"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reasoning"
            },
            {
              "type": "null"
            }
          ]
        },
        "role": {
          "$ref": "#/$defs/Role"
        },
        "tool_call_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "tool_calls": {
          "items": {
            "$ref": "#/$defs/ToolCall"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "required": [
        "role"
      ],
      "title": "DraftMessage",
      "type": "object"
    },
    "Effect": {
      "additionalProperties": false,
      "description": "An in-flight effect (Pending or RetryScheduled) surfaced on each worker decision.\nA flat envelope plus kind-specific fields: a tool call's\n`name`/`arguments`/`handler`, an LLM call's `handler`/`stream`, a\nsub-agent's `agent_id`/`session_id`. A connector sync carries none — its\n`id` is the connection being fetched.",
      "properties": {
        "agent_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "anchor": {
          "description": "The tree node the effect was requested at.",
          "type": [
            "string",
            "null"
          ]
        },
        "arguments": {
          "type": [
            "string",
            "null"
          ]
        },
        "attempt": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "deadline": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "handler": {
          "anyOf": [
            {
              "$ref": "#/$defs/Handler"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/EffectKind"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/EffectStatus"
        },
        "stream": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "tool_call_id": {
          "description": "The model tool call a delegation answers; its own `id` is the child session.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "kind",
        "status",
        "attempt"
      ],
      "title": "Effect",
      "type": "object"
    },
    "EffectKind": {
      "description": "What kind of work an effect is. One enum for the wire and for the engine's\nown scheduling: a decision and a turn's end queue beside the calls and are\nswept the same way, so they are kinds too. Neither ever appears on an\n[`Effect`] — a decision rides the decision list, a turn end has no record.",
      "oneOf": [
        {
          "enum": [
            "tool_call",
            "sub_agent",
            "llm_call"
          ],
          "type": "string"
        },
        {
          "const": "connector_sync",
          "description": "Fetching one connection's tool list. Its `id` is the connection id.",
          "type": "string"
        },
        {
          "const": "decision",
          "description": "A worker decision.",
          "type": "string"
        },
        {
          "const": "turn_end",
          "description": "The turn's completion, dependent on its `turn.finished` finalizer\ndecision settling. Carries the turn id; the frozen output lives in the\nsession's `finalizing`. Never swept: it has no deadline of its own.",
          "type": "string"
        }
      ],
      "title": "EffectKind"
    },
    "EffectStatus": {
      "oneOf": [
        {
          "enum": [
            "pending",
            "completed",
            "failed",
            "retry_scheduled",
            "queued"
          ],
          "type": "string"
        },
        {
          "const": "running",
          "description": "Dispatched and alive, awaiting its result. Off the deadline clock: the\nwork succeeded in starting, and how long it then runs is its own\nbusiness. A delegation sits here for as long as its child turn takes.",
          "type": "string"
        }
      ],
      "title": "EffectStatus"
    },
    "ErrorCode": {
      "description": "What kind of failure, for a consumer that branches instead of reading the\nsentence. A closed set, and required on every [`ErrorInfo`]: an optional\ncode is one nobody fills in, which leaves every consumer handling a `None`\nthat should not exist.\n\n`provider_error`, `rate_limited`, `refused`, `budget_exceeded` and\n`deadline_exceeded` describe a call that ran and went wrong.\n`invalid_response` — a document did not parse, or parsed into something\nunusable. `handler_error` — whoever was asked to do the work (a worker, a\nclient) reported a failure of its own. `worker_unreachable` — it was never\nreached. `unroutable` — nothing could decide. `internal` — the engine's own\nfault, and the honest answer when nothing else fits.",
      "enum": [
        "provider_error",
        "rate_limited",
        "refused",
        "budget_exceeded",
        "deadline_exceeded",
        "invalid_response",
        "handler_error",
        "worker_unreachable",
        "unroutable",
        "internal"
      ],
      "title": "ErrorCode",
      "type": "string"
    },
    "ErrorInfo": {
      "additionalProperties": false,
      "description": "Why something failed. One shape on every event, on the wire, and in the\ninternal carriers that produce them — shaped after a Stripe API error.\n\n`retryable` is deliberately absent: whether to try again is a decision the\nengine makes about one attempt, not a fact about the failure, and it is\nmeaningless on a terminal like `turn.completed`. It rides on the events\nthat settle an attempt instead.",
      "properties": {
        "code": {
          "$ref": "#/$defs/ErrorCode"
        },
        "detail": {
          "description": "Small structured particulars: a status, the llm blocks that exist."
        },
        "message": {
          "description": "One engine-authored sentence, safe to show a human. Never a raw\ndocument — an unbounded body belongs in the log.",
          "type": "string"
        },
        "param": {
          "description": "The one input to go and fix, when the failure names one: `agent.llm`,\n`actions[0].type`. Stripe's `param`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "message",
        "code"
      ],
      "title": "ErrorInfo",
      "type": "object"
    },
    "FileData": {
      "additionalProperties": false,
      "properties": {
        "file_data": {
          "type": "string"
        },
        "filename": {
          "type": "string"
        }
      },
      "required": [
        "filename",
        "file_data"
      ],
      "title": "FileData",
      "type": "object"
    },
    "Handler": {
      "description": "Where a call runs — one wire enum so `handler` has a single type on every\nsurface. Tool calls accept `worker` (default), `client`, or `server` (set by\nthe engine for connector tools, never declared by a worker). An LLM call has\nno `handler`: where it runs follows from the `[llm.*]` block it names.",
      "oneOf": [
        {
          "const": "server",
          "description": "Server-side executor resolves the provider or connection and makes the call.",
          "type": "string"
        },
        {
          "const": "worker",
          "description": "Dispatched to the work queue for the worker to execute.",
          "type": "string"
        },
        {
          "const": "client",
          "description": "Executed by the client. Session goes Idle while waiting (tools only).",
          "type": "string"
        }
      ],
      "title": "Handler"
    },
    "ImageUrl": {
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "title": "ImageUrl",
      "type": "object"
    },
    "InterruptOption": {
      "additionalProperties": false,
      "description": "One enumerated response under `metadata.options` (Slack: a button). A\nclick resumes with the option's `value` as the resolution payload.",
      "properties": {
        "label": {
          "type": "string"
        },
        "style": {
          "description": "`primary` or `danger`; anything else renders plain.",
          "type": [
            "string",
            "null"
          ]
        },
        "value": {
          "description": "Delivered verbatim as the resolution's `payload`; worker vocabulary."
        }
      },
      "required": [
        "label",
        "value"
      ],
      "title": "InterruptOption",
      "type": "object"
    },
    "InterruptOrigin": {
      "description": "Privilege level of the caller that issued an interrupt. Derived from the\nauthenticated `Caller`, never from request data; resuming requires a\ncaller at or above the origin's privilege.",
      "enum": [
        "system",
        "operator",
        "machine",
        "frontend"
      ],
      "title": "InterruptOrigin",
      "type": "string"
    },
    "InterruptPayload": {
      "additionalProperties": false,
      "description": "An interrupt payload following the AG-UI Interrupt shape (spec spelling;\n`id` and `reason` live on the interrupt itself).",
      "properties": {
        "expiresAt": {
          "description": "RFC 3339; display only until engine TTLs land.",
          "type": [
            "string",
            "null"
          ]
        },
        "message": {
          "description": "Markdown; channels down-convert. Without it, channels fall back to\nthe interrupt's `reason`.",
          "type": [
            "string",
            "null"
          ]
        },
        "metadata": {
          "description": "Free-form, delivered to clients verbatim. `metadata.options`\n([`InterruptOption`] list) renders as Slack buttons."
        },
        "responseSchema": {
          "description": "JSON Schema for the expected resolution payload."
        },
        "toolCallId": {
          "description": "Binds the interrupt to a prior tool call.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "title": "InterruptPayload",
      "type": "object"
    },
    "InterruptResolution": {
      "additionalProperties": false,
      "description": "A channel-authored resume payload: the AG-UI resume shape\n(`{status, payload}`) plus a provenance stamp.",
      "properties": {
        "payload": {},
        "responder": {
          "anyOf": [
            {
              "$ref": "#/$defs/InterruptResponder"
            },
            {
              "type": "null"
            }
          ]
        },
        "status": {
          "$ref": "#/$defs/ResumeStatus"
        }
      },
      "required": [
        "status"
      ],
      "title": "InterruptResolution",
      "type": "object"
    },
    "InterruptResponder": {
      "additionalProperties": false,
      "description": "Who resolved it, stamped by the channel — never by the requester.",
      "properties": {
        "channel": {
          "description": "The channel kind, e.g. `slack`, `ag-ui`.",
          "type": "string"
        },
        "label": {
          "description": "The chosen option's label, when the resolution was a pick.",
          "type": [
            "string",
            "null"
          ]
        },
        "style": {
          "description": "The chosen option's `style`, when the resolution was a pick.",
          "type": [
            "string",
            "null"
          ]
        },
        "user": {
          "description": "Channel-native user id.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "channel"
      ],
      "title": "InterruptResponder",
      "type": "object"
    },
    "Issuer": {
      "description": "Where a person's name comes from — `slack`, `app`, `cli`, or whatever a\ndeployment registers. Stamped by whatever authenticated the request, and\nnever read out of one: a caller free to name its own issuer could name\nanother source's people.",
      "title": "Issuer",
      "type": "string"
    },
    "LlmFormat": {
      "description": "The wire shape of a worker-run LLM call, declared on a `type = \"worker\"`\n`[llm.*]` block. Absent ⇒ the engine's neutral format. Set ⇒ `llm.execute`\ncarries the provider's native request body, and `llm.result`/\n`llm.token.delta` accept the provider's native response and stream events.",
      "oneOf": [
        {
          "const": "openai",
          "description": "OpenAI Chat Completions.",
          "type": "string"
        },
        {
          "const": "anthropic",
          "description": "Anthropic Messages API.",
          "type": "string"
        }
      ],
      "title": "LlmFormat"
    },
    "LlmRequest": {
      "additionalProperties": false,
      "properties": {
        "max_completion_tokens": {
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "messages": {
          "items": {
            "$ref": "#/$defs/DraftMessage"
          },
          "type": "array"
        },
        "model": {
          "type": "string"
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReasoningConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "temperature": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "tools": {
          "items": {
            "$ref": "#/$defs/LlmTool"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "required": [
        "model",
        "messages"
      ],
      "title": "LlmRequest",
      "type": "object"
    },
    "LlmResponse": {
      "additionalProperties": false,
      "description": "Normalized LLM response. Provider adapters convert their raw responses\ninto this type at the boundary.",
      "properties": {
        "content": {
          "type": [
            "string",
            "null"
          ]
        },
        "cost": {
          "description": "Cost in dollars for this call, if the provider reports it. A decimal\nstring on the wire.",
          "pattern": "^-?\\d+(\\.\\d+)?$",
          "type": [
            "string",
            "null"
          ]
        },
        "finish_reason": {
          "type": [
            "string",
            "null"
          ]
        },
        "images": {
          "description": "Images generated by the model.",
          "items": {
            "$ref": "#/$defs/ResponseImage"
          },
          "type": "array"
        },
        "model": {
          "type": "string"
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reasoning"
            },
            {
              "type": "null"
            }
          ]
        },
        "tool_calls": {
          "items": {
            "$ref": "#/$defs/ToolCall"
          },
          "type": "array"
        },
        "usage": {
          "anyOf": [
            {
              "$ref": "#/$defs/Usage"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "model"
      ],
      "title": "LlmResponse",
      "type": "object"
    },
    "LlmTool": {
      "additionalProperties": false,
      "description": "A tool's declared contract: flat on the wire. Providers that need\nOpenAI-style `{\"type\": \"function\", \"function\": {…}}` nesting re-wrap at\ntheir own boundary.",
      "properties": {
        "defer": {
          "description": "Keep this definition out of the request.\n\nThe engine still records it, still routes a call to it, and still finds\nit in a search. Only the request omits it, which is what keeps a large\ntool set out of the model's context and out of the cached prefix.\n\nAny source can set it: a tool the config declares, a connection, or\nwhatever comes next. Deferral is a property of a tool, not of where it\ncame from.",
          "type": "boolean"
        },
        "description": {
          "type": "string"
        },
        "input": {
          "description": "JSON Schema for the tool's arguments; omitted declares a no-argument\ntool. The engine validates each call's arguments against it and hands\nproviders their native form."
        },
        "name": {
          "type": "string"
        },
        "output": {
          "description": "JSON Schema the settled result must satisfy; never sent to the model.\nA violating result settles as a terminal tool error."
        }
      },
      "required": [
        "name",
        "description"
      ],
      "title": "LlmTool",
      "type": "object"
    },
    "McpAnnounce": {
      "description": "Where an MCP announcement lands.",
      "oneOf": [
        {
          "const": "auto",
          "description": "The system prompt while no call has dispatched; then a block on the\nlast user message; then a message of its own. The engine takes the\nfirst place it can use, so the order is not a setting.",
          "type": "string"
        },
        {
          "const": "never",
          "description": "Nowhere. For a server whose own words help nobody.",
          "type": "string"
        }
      ],
      "title": "McpAnnounce"
    },
    "McpAuthFailure": {
      "description": "What a session does when a connection needs a person to authorize it. It\nbelongs to the pair: one credential serves an agent that stops and asks, and\nan agent that has nobody to ask.",
      "oneOf": [
        {
          "const": "interrupt",
          "description": "Stop and ask. A channel that cannot show the question degrades instead.",
          "type": "string"
        },
        {
          "const": "degrade",
          "description": "Go on without this connection's tools.",
          "type": "string"
        }
      ],
      "title": "McpAuthFailure"
    },
    "McpServer": {
      "additionalProperties": false,
      "description": "An MCP server the agent draws tools from. `path` resolves against the\nengine's connection registry — locally from `substructure.toml`, in the\ncloud from the connections an admin granted this app. The worker never names\na URL or a credential.",
      "properties": {
        "approve": {
          "$ref": "#/$defs/Approve"
        },
        "auth_failure": {
          "$ref": "#/$defs/McpAuthFailure"
        },
        "path": {
          "$ref": "#/$defs/ConnectionPath"
        },
        "tool_sync_failure": {
          "$ref": "#/$defs/McpToolSyncFailure"
        },
        "tools": {
          "anyOf": [
            {
              "$ref": "#/$defs/McpTools"
            },
            {
              "type": "null"
            }
          ],
          "description": "Narrows what the model sees. Absent ⇒ every tool the connection grants."
        }
      },
      "required": [
        "path"
      ],
      "title": "McpServer",
      "type": "object"
    },
    "McpToolSyncFailure": {
      "description": "What a session does when a connection's tool fetch fails for the last time.\nThe turn goes ahead without those tools either way, because only the agent\nknows whether it can work without them. This chooses whether the model is\ntold, or is left to read their absence as nothing being there.",
      "oneOf": [
        {
          "const": "warn",
          "description": "Name the connection wherever its tools would have been.",
          "type": "string"
        },
        {
          "const": "silent",
          "description": "Say nothing. For a connection the agent does not need.",
          "type": "string"
        }
      ],
      "title": "McpToolSyncFailure"
    },
    "McpTools": {
      "additionalProperties": false,
      "description": "What the model sees of one connection, for one agent: which tools, and how\nthey reach the model.\n\nThe filter is applied in order — capability predicates, then `include`, then\n`exclude` — and only ever narrowing, so a filter can never widen what the\nconnection grants. `defer` runs after it and removes nothing.\n\n`include`/`exclude` are globs matched against the tool's name on the\nconnection, the name its own documentation uses, not the prefixed name the\nmodel sees. Capability predicates read the MCP annotations; a tool that\ncarries none fails the predicate, so an unannotated server yields nothing\nunder `read_only` rather than silently passing everything through.",
      "properties": {
        "defer": {
          "description": "Keep every surviving tool out of the request. See [`LlmTool::defer`].\nAbsent ⇒ the agent's `defer_tools`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "exclude": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "idempotent": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "include": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "non_destructive": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "read_only": {
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "title": "McpTools",
      "type": "object"
    },
    "Message": {
      "additionalProperties": false,
      "properties": {
        "content": {
          "anyOf": [
            {
              "$ref": "#/$defs/Content"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "reasoning": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reasoning"
            },
            {
              "type": "null"
            }
          ]
        },
        "role": {
          "$ref": "#/$defs/Role"
        },
        "tool_call_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "tool_calls": {
          "items": {
            "$ref": "#/$defs/ToolCall"
          },
          "type": "array"
        }
      },
      "required": [
        "id",
        "role"
      ],
      "title": "Message",
      "type": "object"
    },
    "MessageTree": {
      "additionalProperties": false,
      "properties": {
        "head_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "nodes": {
          "items": {
            "additionalProperties": false,
            "properties": {
              "message": {
                "$ref": "#/$defs/Message"
              },
              "parent_id": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "message"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "nodes"
      ],
      "title": "MessageTree",
      "type": "object"
    },
    "Reasoning": {
      "additionalProperties": false,
      "description": "What the model thought before it answered. `text` is for a reader; `blocks`\nare the provider's own, held verbatim because Anthropic requires the\nthinking that precedes a tool call back unmodified, signature included.",
      "properties": {
        "blocks": {
          "items": {},
          "type": "array"
        },
        "provider": {
          "$ref": "#/$defs/ReasoningProvider"
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "provider"
      ],
      "title": "Reasoning",
      "type": "object"
    },
    "ReasoningConfig": {
      "additionalProperties": false,
      "properties": {
        "effort": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReasoningEffort"
            },
            {
              "type": "null"
            }
          ]
        },
        "enabled": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "exclude": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "max_tokens": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "title": "ReasoningConfig",
      "type": "object"
    },
    "ReasoningEffort": {
      "enum": [
        "xhigh",
        "high",
        "medium",
        "low",
        "minimal",
        "none"
      ],
      "title": "ReasoningEffort",
      "type": "string"
    },
    "ReasoningProvider": {
      "description": "Which provider wrote a [`Reasoning`]'s blocks. They ride back only to it:\nanother provider reads them as noise, and Anthropic rejects blocks it did\nnot sign.",
      "enum": [
        "anthropic",
        "openai",
        "openrouter"
      ],
      "title": "ReasoningProvider",
      "type": "string"
    },
    "ResourceContents": {
      "additionalProperties": false,
      "properties": {
        "blob": {
          "type": [
            "string",
            "null"
          ]
        },
        "mimeType": {
          "type": [
            "string",
            "null"
          ]
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "type": "string"
        }
      },
      "required": [
        "uri"
      ],
      "title": "ResourceContents",
      "type": "object"
    },
    "ResponseImage": {
      "additionalProperties": false,
      "description": "An image returned by the model in the response.",
      "properties": {
        "url": {
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "title": "ResponseImage",
      "type": "object"
    },
    "ResumeStatus": {
      "enum": [
        "resolved",
        "cancelled"
      ],
      "title": "ResumeStatus",
      "type": "string"
    },
    "RetryConfig": {
      "additionalProperties": false,
      "description": "An agent's retry overrides, one per effect kind. `default` covers the kinds\nthat name nothing; a kind is layered on top of it, so the two compose.\n\nPer kind because the kinds are not alike: an LLM call is idempotent and worth\nretrying, a tool call may not be, and a connector fetch holds up every\ndecision behind it.",
      "properties": {
        "connector": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryOverride"
            },
            {
              "type": "null"
            }
          ]
        },
        "default": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryOverride"
            },
            {
              "type": "null"
            }
          ]
        },
        "llm": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryOverride"
            },
            {
              "type": "null"
            }
          ]
        },
        "sub_agent": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryOverride"
            },
            {
              "type": "null"
            }
          ]
        },
        "tool": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryOverride"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "title": "RetryConfig",
      "type": "object"
    },
    "RetryOverride": {
      "additionalProperties": false,
      "description": "Only the fields it names change; the rest are inherited. An override cannot\nset a timeout back to unbounded.",
      "properties": {
        "backoff_base_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "backoff_max_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_attempts": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "queue_timeout_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "run_timeout_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "total_timeout_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "title": "RetryOverride",
      "type": "object"
    },
    "RetryPolicy": {
      "additionalProperties": false,
      "properties": {
        "backoff_base_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "backoff_max_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_attempts": {
          "description": "Cap on total attempts, not on retries: `1` allows one try and no retry.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "queue_timeout_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "run_timeout_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "total_timeout_secs": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "max_attempts",
        "backoff_base_secs",
        "backoff_max_secs"
      ],
      "title": "RetryPolicy",
      "type": "object"
    },
    "Role": {
      "enum": [
        "system",
        "user",
        "assistant",
        "tool"
      ],
      "title": "Role",
      "type": "string"
    },
    "SkillMeta": {
      "additionalProperties": false,
      "description": "What the model sees of a skill before it loads it.",
      "properties": {
        "description": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "title": "SkillMeta",
      "type": "object"
    },
    "StoredContent": {
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "text": {
              "type": "string"
            },
            "type": {
              "const": "text",
              "type": "string"
            }
          },
          "required": [
            "type",
            "text"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "type": {
              "const": "blob",
              "type": "string"
            },
            "uri": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "uri"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "mimeType": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "link",
              "type": "string"
            },
            "uri": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "uri"
          ],
          "type": "object"
        }
      ],
      "title": "StoredContent"
    },
    "StoredResult": {
      "additionalProperties": false,
      "properties": {
        "content": {
          "items": {
            "$ref": "#/$defs/StoredContent"
          },
          "type": "array"
        },
        "isError": {
          "type": "boolean"
        },
        "structuredContent": {}
      },
      "title": "StoredResult",
      "type": "object"
    },
    "StreamDelta": {
      "additionalProperties": false,
      "properties": {
        "finish_reason": {
          "type": [
            "string",
            "null"
          ]
        },
        "reasoning": {
          "type": [
            "string",
            "null"
          ]
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        },
        "tool_calls": {
          "items": {
            "$ref": "#/$defs/ToolCallChunk"
          },
          "type": "array"
        }
      },
      "title": "StreamDelta",
      "type": "object"
    },
    "SubAgent": {
      "additionalProperties": false,
      "description": "A sub-agent the model can delegate to. Named by `id` (the child agent to spawn,\nand the tool name the model calls); its model-facing input is the conventional\nsingle-`message` delegation schema.",
      "properties": {
        "description": {
          "type": "string"
        },
        "id": {
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "title": "SubAgent",
      "type": "object"
    },
    "Subject": {
      "additionalProperties": false,
      "description": "One identity, as the source that authenticated it named it: OIDC's\n`(iss, sub)`. An id means nothing without its issuer, because it is only\nunique within one.",
      "properties": {
        "id": {
          "type": "string"
        },
        "issuer": {
          "$ref": "#/$defs/Issuer"
        }
      },
      "required": [
        "issuer",
        "id"
      ],
      "title": "Subject",
      "type": "object"
    },
    "TokenDelta": {
      "additionalProperties": false,
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "attempt": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "call_id": {
          "type": "string"
        },
        "finish_reason": {
          "type": [
            "string",
            "null"
          ]
        },
        "reasoning": {
          "type": [
            "string",
            "null"
          ]
        },
        "root_session_id": {
          "description": "Transport routing key.",
          "type": "string"
        },
        "seq": {
          "description": "Per-call counter, distinct from event-store sequence.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "session_id": {
          "description": "May be a sub-agent of root.",
          "type": "string"
        },
        "tenant_id": {
          "description": "Tenant isolation key — subscribers must match.",
          "type": "string"
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        },
        "tool_calls": {
          "items": {
            "$ref": "#/$defs/ToolCallChunk"
          },
          "type": "array"
        },
        "turn_id": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "tenant_id",
        "root_session_id",
        "session_id",
        "agent_id",
        "call_id",
        "attempt",
        "seq",
        "tool_calls"
      ],
      "title": "TokenDelta",
      "type": "object"
    },
    "ToolCall": {
      "additionalProperties": false,
      "properties": {
        "function": {
          "$ref": "#/$defs/ToolCallFunction"
        },
        "id": {
          "type": "string"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "type",
        "function"
      ],
      "title": "ToolCall",
      "type": "object"
    },
    "ToolCallChunk": {
      "additionalProperties": false,
      "properties": {
        "arguments": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id"
      ],
      "title": "ToolCallChunk",
      "type": "object"
    },
    "ToolCallFunction": {
      "additionalProperties": false,
      "properties": {
        "arguments": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "arguments"
      ],
      "title": "ToolCallFunction",
      "type": "object"
    },
    "ToolContent": {
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "text": {
              "type": "string"
            },
            "type": {
              "const": "text",
              "type": "string"
            }
          },
          "required": [
            "type",
            "text"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "data": {
              "type": "string"
            },
            "mimeType": {
              "type": "string"
            },
            "type": {
              "const": "image",
              "type": "string"
            }
          },
          "required": [
            "type",
            "data",
            "mimeType"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "data": {
              "type": "string"
            },
            "mimeType": {
              "type": "string"
            },
            "type": {
              "const": "audio",
              "type": "string"
            }
          },
          "required": [
            "type",
            "data",
            "mimeType"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "resource": {
              "$ref": "#/$defs/ResourceContents"
            },
            "type": {
              "const": "resource",
              "type": "string"
            }
          },
          "required": [
            "type",
            "resource"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "mimeType": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "const": "resource_link",
              "type": "string"
            },
            "uri": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "uri"
          ],
          "type": "object"
        }
      ],
      "title": "ToolContent"
    },
    "ToolInput": {
      "description": "The engine's classification of a tool call's arguments, delivered on the\n`tool.execute` trigger alongside the raw `arguments` string. Always on the\nwire — absence never carries meaning.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Parsed and, when the tool declares an `input` schema, conforming to it.\n`value` is exactly the parsed `arguments` — the engine never mutates it.",
          "properties": {
            "status": {
              "const": "valid",
              "type": "string"
            },
            "value": {}
          },
          "required": [
            "status",
            "value"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Parsed to an object that violates the declared `input` schema.",
          "properties": {
            "error": {
              "type": "string"
            },
            "status": {
              "const": "invalid",
              "type": "string"
            },
            "value": {}
          },
          "required": [
            "status",
            "value",
            "error"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Not a JSON object: malformed JSON or a non-object value.",
          "properties": {
            "error": {
              "type": "string"
            },
            "status": {
              "const": "malformed",
              "type": "string"
            }
          },
          "required": [
            "status",
            "error"
          ],
          "type": "object"
        }
      ],
      "title": "ToolInput"
    },
    "Usage": {
      "additionalProperties": false,
      "description": "What one call read and wrote, in counts every provider means the same way.\n\nEach vendor names and scopes these differently: Anthropic reports the part\nof the prompt it did not read from the cache, OpenAI reports the whole\nprompt including that part. A session that changes model, and a tree whose\nagents name different blocks, add these counts together, so the adapter\nnormalizes them rather than the reader.",
      "properties": {
        "cache_read": {
          "description": "The part of `input` the provider read from the cache.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "cache_write": {
          "description": "The part of `input` the provider wrote to the cache.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "input": {
          "description": "Every input token of the call, cached or not.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "output": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "provider": {
          "description": "The counts as the provider reported them, for a reader that wants a\nnumber this type does not name."
        },
        "total": {
          "description": "`input` and `output` together.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "uncached_input": {
          "description": "The part of `input` the provider read fresh.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "input",
        "output",
        "uncached_input",
        "cache_read",
        "cache_write",
        "total"
      ],
      "title": "Usage",
      "type": "object"
    },
    "VideoUrl": {
      "additionalProperties": false,
      "properties": {
        "url": {
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "title": "VideoUrl",
      "type": "object"
    },
    "Visibility": {
      "description": "Who can read what a session says. The transport sets it once, at the\nsession's start; everything absent or unknown reads as `shared`, because\n`shared` is the value that never selects a personal credential.\n\nNot OAuth's `aud`, which names a resource server rather than a readership.",
      "oneOf": [
        {
          "const": "shared",
          "description": "More than one person can read the answer.",
          "type": "string"
        },
        {
          "const": "private",
          "description": "One person only.",
          "type": "string"
        }
      ],
      "title": "Visibility"
    },
    "WorkerIdentity": {
      "additionalProperties": false,
      "description": "The owner as delivered to the worker on `DecisionRequest.identity`, without\nthe tenant. Read `kind` with `id`: only `frontend` is an end user.",
      "properties": {
        "metadata": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "subject": {
          "anyOf": [
            {
              "$ref": "#/$defs/Subject"
            },
            {
              "type": "null"
            }
          ]
        },
        "visibility": {
          "$ref": "#/$defs/Visibility"
        }
      },
      "required": [
        "metadata"
      ],
      "title": "WorkerIdentity",
      "type": "object"
    },
    "WorkerState": {
      "description": "Opaque worker state: JSON the engine stores but never interprets.",
      "title": "WorkerState"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "One entry point per wire surface; every protocol type is named under $defs.",
  "properties": {
    "client_input": {
      "$ref": "#/$defs/ClientInput"
    },
    "client_payload": {
      "$ref": "#/$defs/ClientPayload"
    },
    "decision_request": {
      "$ref": "#/$defs/DecisionRequest"
    },
    "decision_response": {
      "$ref": "#/$defs/DecisionResponse"
    },
    "interrupt_payload": {
      "$ref": "#/$defs/InterruptPayload"
    },
    "interrupt_resolution": {
      "$ref": "#/$defs/InterruptResolution"
    },
    "stream_delta": {
      "$ref": "#/$defs/StreamDelta"
    },
    "token_delta": {
      "$ref": "#/$defs/TokenDelta"
    }
  },
  "title": "Substructure protocol",
  "type": "object"
}
