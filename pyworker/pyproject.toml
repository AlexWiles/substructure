[project]
name = "pyworker"
version = "0.1.0"
description = "Python gRPC worker for Substructure"
requires-python = ">=3.11"
dependencies = [
    "grpcio>=1.68",
    "grpcio-tools>=1.68",
    "protobuf>=5.29",
]
